install.packages("devtools")
install.packages("roxygen2")
devtools::document()

set.seed(123)
X <- cbind(1, matrix(rnorm(200), ncol = 2))  # n = 100, p = 2 + intercept
beta <- c(2, 3, -1)
y <- X %*% beta + rnorm(100, 0, 1)

model <- ols_fit(X, y)
predict_ols(model, X[1:5, ])
